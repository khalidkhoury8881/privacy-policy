let items=[];const $=s=>document.querySelector(s);function save(){chrome.runtime.sendMessage({type:'UPDATE_REMINDERS',items},render)}function tomorrow9(){const d=new Date();d.setDate(d.getDate()+1);d.setHours(9,0,0,0);return d.getTime()}function render(){const box=$('#items'); if(!items.length){box.textContent='No saved reminders yet.';return} box.innerHTML=''; items.forEach(item=>{const el=document.createElement('article'); el.className='item'; el.innerHTML=`<strong></strong><div class="meta"></div><p></p><div class="row"><button data-action="open" class="secondary">Open source</button><button data-action="tried">Mark tried</button><button data-action="hour" class="secondary">Remind in 1 hour</button><button data-action="tomorrow" class="secondary">Tomorrow 9 AM</button><button data-action="delete" class="danger">Delete</button></div>`; el.querySelector('strong').textContent=item.title; el.querySelector('.meta').textContent=`${formatDateTime(item.reminderAt)}${item.tried?' • tried':''}${item.url?' • '+item.url:''}`; el.querySelector('p').textContent=item.note||''; el.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>act(item.id,b.dataset.action))); box.append(el)})}function act(id,action){const item=items.find(i=>i.id===id); if(!item)return; if(action==='open'&&item.url){chrome.tabs.create({url:item.url});return} if(action==='tried') item.tried=true; if(action==='hour'){item.reminderAt=Date.now()+3600000; item.notifiedAt=0; item.tried=false} if(action==='tomorrow'){item.reminderAt=tomorrow9(); item.notifiedAt=0; item.tried=false} if(action==='delete') items=items.filter(i=>i.id!==id); save()}document.addEventListener('DOMContentLoaded',()=>chrome.runtime.sendMessage({type:'GET_REMINDERS'},r=>{items=r?.items||[]; render()}));
